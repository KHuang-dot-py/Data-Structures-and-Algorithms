def maxSubarraySum(nums: list[int], k: int) -> int:
    # intuitively, since we are exploring all possibilities, it is not going to be possible to find a linear solution.
    # sorting in python is nlogn (timsort)
    

a_rray = [-11,2,5,4]
my_k = 3

maxSubarraySum(nums = a_rray,k = 3)